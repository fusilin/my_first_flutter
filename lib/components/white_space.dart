import 'package:flutter/material.dart';

Container WhiteSpace(double height, Color color) {
  return new Container(
      height: height ?? 10.0,
      color: color ?? new Color.fromARGB(255, 242, 242, 245));
}
